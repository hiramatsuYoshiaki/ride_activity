enum ApplicationLoginState {
  loggedOut,
  emailAdress,
  register,
  password,
  welcom,
  sendEmail,
  loggedIn,
  passReset
}

enum ProfileState {
  photoUpdate,
  dispalyNameUpdate,
  emailUpdate,
  display,
}

enum AuthState {
  loggedIn,
  loggedOut,
}

enum loadState {
  loading,
  waiting,
}

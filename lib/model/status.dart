enum ApplicationLoginState {
  loggedOut,
  emailAdress,
  register,
  password,
  welcom,
  sendEmail,
  loggedIn,
  passReset
}

enum ProfileState {
  photoUpdate,
  dispalyNameUpdate,
  emailUpdate,
  display,
  removeAccount
}

enum ActivityState {
  display,
  activityDetail,
  activityAdd,
  activityRemove,
  activityEdit,
  activityDone,
}

enum RiderState {
  display,
  riderDetail,
  riderAdd,
  riderRemove,
  riderEdit,
}

enum AuthState {
  loggedIn,
  loggedOut,
}

enum loadState {
  loading,
  waiting,
}

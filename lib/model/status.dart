enum ApplicationLoginState {
  loggedOut,
  emailAdress,
  register,
  password,
  welcom,
  sendEmail,
  loggedIn,
  passReset
}

enum AuthState {
  loggedIn,
  loggedOut,
}

enum loadState {
  loading,
  waiting,
}

import 'package:flutter/material.dart';

class DisplayNameUpdate extends StatelessWidget {
  const DisplayNameUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('DisplayNameUpdate'),
    );
  }
}

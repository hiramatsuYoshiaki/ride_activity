import 'package:flutter/material.dart';

class PlanDetail extends StatelessWidget {
  const PlanDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Plan Detael'));
  }
}

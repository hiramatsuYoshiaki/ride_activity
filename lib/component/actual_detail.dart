import 'package:flutter/material.dart';

class ActualDetail extends StatelessWidget {
  const ActualDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Actual detail'));
  }
}
